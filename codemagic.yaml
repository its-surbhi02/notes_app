workflows:
  release-apk:
    name: Build APK
    environment:
      groups:
        - keystore_credentials  
      flutter: stable
    scripts:
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
